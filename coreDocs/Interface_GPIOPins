GPIOPins -- interface
    This interface should be implemented as an enum class or similar where ordinals correspond to
    their hardware pin numbers.


Data:
    1. enumerated pins - *Note* - this should be implemented at the Platform level

Does:
Doesn't:
